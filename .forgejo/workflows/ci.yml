name: PHP Composer

on:
    push:
        branches: [ develop ]
    pull_request:
        branches: [ develop ]

jobs:
    build:

        runs-on: ubuntu-latest
        env:
            COMPOSER_NO_DEV: 1

        steps:
        - uses: actions/checkout@v3